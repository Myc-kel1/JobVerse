import { PlaceholderPage } from "@/components/PlaceholderPage";
import { PageMeta } from "@/components/PageMeta";

export default function GeneratePage() {
  return (
    <>
      <PageMeta title="Generate — Jobverse" description="Manual, on-demand search and drafting actions will live here." />
      <PlaceholderPage active="Generate" title="Generate" note="Manual, on-demand search and drafting actions will live here." />
    </>
  );
}
